language: ruby

rvm:
  - 2.2.3
  - ruby-head
  - rbx-2

env:
  global: JRUBY_OPTS=--dev

script:
  - bundle exec rake ${TEST_SUITE:-test}

matrix:
  allow_failures:
    - rvm: ruby-head
    - rvm: rbx-2
    - rvm: jruby
